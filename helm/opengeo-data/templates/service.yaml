apiVersion: v1
kind: Service
metadata:
  name: {{ include "opengeo-data.fullname" . }}
  labels:
    {{- include "opengeo-data.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "opengeo-data.selectorLabels" . | nindent 4 }}
